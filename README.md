# React Todolist ✅

A clean and lightweight task management app built with **React**, **Vite**, and **JavaScript**.

## ⚙️ Tech Stack

- ⚛️ [React](https://react.dev/)
- ⚡ [Vite](https://vitejs.dev/)
- 🟨 JavaScript (ES6+)
- 🎨 HTML + CSS

## 📦 Setup

Clone the repository and install dependencies using `pnpm`, `npm`, or `yarn`.

# Clone the repo
git clone https://github.com/Alvesdtd/React-Todolist.git
cd React-Todolist

# Install dependencies
pnpm install

# Start development server
pnpm dev

## 📁 Folder Structure
src/
├── components/       # Reusable components
│   └── CustomForm.jsx
├── App.jsx           # Root component
├── main.jsx          # React entry point
├── index.css         # Global styles

## ✨ Features

Add new tasks

Display task list

Mark tasks as completed

Delete tasks

## 🎯 Learning Goals

This project was built to:

Practice component-based architecture with React

Use useState for reactive state management

Understand Vite project structure and build process

Apply accessibility principles (aria-label, htmlFor, etc.)

## ✅ Requirements

Node.js 18+ recommended

pnpm / npm / yarn
